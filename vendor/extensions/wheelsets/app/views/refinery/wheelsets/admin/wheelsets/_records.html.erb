<%= render 'refinery/admin/search_header', :url => refinery.wheelsets_admin_wheelsets_path %>
<div class='pagination_container'>
  <% if @wheelsets.any? %>
    <%= render 'wheelsets' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
