<% if @wheelset.price %>
  <%= button_to 'Buy', '#',
    class: "snipcart-add-item btn btn-lg btn-success btn-block",
    data: {:item_name => @wheelset.name,
           :item_id => @wheelset.id,
           :item_price => @wheelset.price,
           :item_weight => @wheel.shipping_weight,
           :item_url => "/wheelsets/" + @wheelset.slug,
           :item_custom2_name => 'Campagnolo or Shimano-SRAM',
           :item_custom2_options => 'Campagnolo | Shimano',
           :item_description => @wheelset.description
          }
  %>
<% end %>