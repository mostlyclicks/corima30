<% if @wheel.wheelset_price %>
  <%= link_to 'Buy', '#',
    class: "snipcart-add-item",
    data: {:item_name => @wheel.name,
           :item_id => @wheel.id,
           :item_price => @wheel.wheelset_price,
           :item_weight => @wheel.shipping_weight,
           :item_url => "/wheels/" + @wheel.slug,
           :item_description => @wheel.wheelset_description
          }
  %>
<% end %>